{
  "name": "gpt-project-hub",
  "private": true,
  "scripts": {
    "sync": "./scripts/sync-docs.sh",
    "build:preview": "./scripts/build-preview.sh"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
